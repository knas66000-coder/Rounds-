import { useEffect, useMemo, useState } from "react";
import { ActivityIndicator, FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { useRouter } from "expo-router";

import { ScreenContainer } from "@/components/screen-container";
import { useColors } from "@/hooks/use-colors";
import { haptic } from "@/lib/haptics";
import { HIGH_SCHOOL_LEVELS, highSchoolLevelLabel, loadHighSchoolLevel, loadHighSchoolRevisionPlan, saveHighSchoolLevel, saveHighSchoolRevisionPlan, type HighSchoolLevel, type HighSchoolRevisionPlan } from "@/lib/high-school-store";
import { isPackInstalled, loadCoursePackInstalls, type CoursePackInstall } from "@/lib/course-pack-store";
import { courseRoundSnapshot, loadCourseRoundState, type CourseRoundState } from "@/lib/course-round-store";
import { trpc } from "@/lib/trpc";
import { isUgandaHighSchoolProgram } from "@/shared/academic-profile";
import { highSchoolCoursePacks, type CoursePack } from "@/shared/course-packs";

const PACK_ACCENTS: Record<string, string> = {
  "uganda-high-school-biology": "#39735D", "uganda-high-school-chemistry": "#5A5E9F", "uganda-high-school-economics": "#8B5E39", "uganda-high-school-entrepreneurship": "#986543", "uganda-high-school-english": "#6E527D", "uganda-high-school-physics": "#3D6D92", "uganda-high-school-mathematics": "#8C4C5F", "uganda-high-school-geography": "#547B69", "uganda-high-school-history-civics": "#7B6150", "uganda-high-school-ict": "#386D83", "uganda-high-school-agriculture": "#6B7D40", "uganda-high-school-religion-ethics": "#765A7C",
};

const fallbackRoundState: CourseRoundState = { records: [], bookmarks: [] };

export default function HighSchoolHomeScreen() {
  const colors = useColors();
  const router = useRouter();
  const profileQuery = trpc.academicProfile.get.useQuery();
  const profile = profileQuery.data;
  const [level, setLevel] = useState<HighSchoolLevel>("s1");
  const [installs, setInstalls] = useState<CoursePackInstall[]>([]);
  const [revision, setRevision] = useState<HighSchoolRevisionPlan>({ focusPackId: null, weeklyTarget: 3, updatedAt: null });
  const [roundState, setRoundState] = useState<CourseRoundState>(fallbackRoundState);
  const [ready, setReady] = useState(false);
  const packs = useMemo(() => highSchoolCoursePacks(), []);
  const focusPack = packs.find((pack) => pack.id === revision.focusPackId) ?? null;
  const overallCompleted = packs.reduce((sum, pack) => sum + courseRoundSnapshot(pack.id, roundState).completed, 0);
  const overallSaved = packs.reduce((sum, pack) => sum + courseRoundSnapshot(pack.id, roundState).saved, 0);

  useEffect(() => {
    void Promise.all([loadHighSchoolLevel(), loadCoursePackInstalls(), loadHighSchoolRevisionPlan(), loadCourseRoundState()]).then(([nextLevel, nextInstalls, nextRevision, nextRoundState]) => {
      setLevel(nextLevel); setInstalls(nextInstalls); setRevision(nextRevision); setRoundState(nextRoundState); setReady(true);
    });
  }, []);

  if (!profile || !isUgandaHighSchoolProgram(profile.program)) return null;

  const chooseLevel = async (next: HighSchoolLevel) => { haptic.medium(); setLevel(next); await saveHighSchoolLevel(next); };
  const chooseRevisionFocus = async (packId: string) => { haptic.light(); const next = await saveHighSchoolRevisionPlan({ focusPackId: packId, weeklyTarget: revision.weeklyTarget }); setRevision(next); };
  const changeWeeklyTarget = async () => { haptic.light(); const weeklyTarget = revision.weeklyTarget === 4 ? 2 : revision.weeklyTarget + 1 as 2 | 3 | 4; const next = await saveHighSchoolRevisionPlan({ focusPackId: revision.focusPackId, weeklyTarget }); setRevision(next); };
  const openPack = (pack: CoursePack, review = false) => {
    haptic.light();
    if (!isPackInstalled(pack, installs)) { router.push({ pathname: "/course-packs", params: { focus: pack.id, level } } as never); return; }
    router.push({ pathname: "/course-round", params: { packId: pack.id, level, ...(review ? { review: "1" } : {}) } } as never);
  };

  return (
    <ScreenContainer className="px-5" edges={["top", "left", "right"]}>
      <FlatList
        data={packs}
        keyExtractor={(pack) => pack.id}
        numColumns={2}
        columnWrapperStyle={styles.columns}
        contentContainerStyle={styles.content}
        showsVerticalScrollIndicator={false}
        ListHeaderComponent={<View style={styles.header}><Text style={[styles.eyebrow, { color: colors.primary }]}>UGANDA HIGH SCHOOL</Text><Text style={[styles.title, { color: colors.foreground }]}>Your complete study space.</Text><Text style={[styles.sub, { color: colors.muted }]}>{profile.institutionName} · choose your level, add a subject locally, then learn offline in Rounds.</Text><View style={[styles.levelSheet, { backgroundColor: colors.surface, borderColor: colors.border }]}><View><Text style={[styles.levelLabel, { color: colors.primary }]}>CURRENT STUDY LEVEL</Text><Text style={[styles.levelTitle, { color: colors.foreground }]}>{highSchoolLevelLabel(level)}</Text></View><Text style={[styles.levelNote, { color: colors.muted }]}>Your level stays private on this device. Starter content remains original learning practice and does not claim official assessment coverage.</Text><FlatList horizontal data={HIGH_SCHOOL_LEVELS} keyExtractor={(item) => item.id} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.levelList} renderItem={({ item }) => <Pressable onPress={() => void chooseLevel(item.id)} accessibilityRole="radio" accessibilityState={{ selected: level === item.id }} style={({ pressed }) => [styles.levelChip, { borderColor: level === item.id ? colors.primary : colors.border, backgroundColor: level === item.id ? colors.primary : colors.background }, pressed && styles.pressed]}><Text style={[styles.levelChipTitle, { color: level === item.id ? colors.background : colors.foreground }]}>{item.title}</Text><Text style={[styles.levelChipBand, { color: level === item.id ? colors.background : colors.muted }]}>{item.band}</Text></Pressable>} /></View><View style={[styles.progressStrip, { borderColor: colors.border, backgroundColor: colors.surface }]}><View><Text style={[styles.progressNumber, { color: colors.foreground }]}>{overallCompleted}</Text><Text style={[styles.progressLabel, { color: colors.muted }]}>ACTIVITIES REVIEWED</Text></View><View><Text style={[styles.progressNumber, { color: colors.foreground }]}>{overallSaved}</Text><Text style={[styles.progressLabel, { color: colors.muted }]}>SAVED FOR REVIEW</Text></View><View><Text style={[styles.progressNumber, { color: colors.foreground }]}>{ready ? installs.filter((entry) => packs.some((pack) => pack.id === entry.packId)).length : "–"}</Text><Text style={[styles.progressLabel, { color: colors.muted }]}>OFFLINE PACKS</Text></View></View><View style={styles.actionRow}><Pressable onPress={() => { haptic.light(); router.push({ pathname: "/course-packs", params: { level } } as never); }} accessibilityRole="button" style={({ pressed }) => [styles.libraryButton, { borderColor: colors.primary }, pressed && styles.pressed]}><Text style={[styles.libraryButtonText, { color: colors.primary }]}>Manage offline subjects</Text><Text style={[styles.actionChevron, { color: colors.primary }]}>›</Text></Pressable><Pressable onPress={() => { haptic.light(); router.push("/academic-onboarding" as never); }} accessibilityRole="button" style={({ pressed }) => [styles.profileButton, { borderColor: colors.border }, pressed && styles.pressed]}><Text style={[styles.profileButtonText, { color: colors.foreground }]}>Study profile</Text><Text style={[styles.actionChevron, { color: colors.muted }]}>›</Text></Pressable></View><View style={[styles.revisionSheet, { borderColor: colors.border, backgroundColor: colors.surface }]}><View style={styles.revisionTop}><View><Text style={[styles.revisionLabel, { color: colors.primary }]}>REVISION PLAN</Text><Text style={[styles.revisionTitle, { color: colors.foreground }]}>{focusPack ? `${focusPack.title.replace("High School ", "")} focus` : "Choose a revision focus"}</Text></View><Pressable onPress={() => void changeWeeklyTarget()} accessibilityRole="button" style={({ pressed }) => [styles.targetButton, { borderColor: colors.primary }, pressed && styles.pressed]}><Text style={[styles.targetButtonText, { color: colors.primary }]}>{revision.weeklyTarget} sessions / week</Text></Pressable></View><FlatList horizontal data={packs} keyExtractor={(pack) => pack.id} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.revisionList} renderItem={({ item }) => <Pressable onPress={() => void chooseRevisionFocus(item.id)} accessibilityRole="radio" accessibilityState={{ selected: item.id === revision.focusPackId }} style={({ pressed }) => [styles.revisionChip, { borderColor: item.id === revision.focusPackId ? PACK_ACCENTS[item.id] ?? colors.primary : colors.border, backgroundColor: item.id === revision.focusPackId ? PACK_ACCENTS[item.id] ?? colors.primary : colors.background }, pressed && styles.pressed]}><Text style={[styles.revisionChipText, { color: item.id === revision.focusPackId ? colors.background : colors.foreground }]}>{item.title.replace("High School ", "")}</Text></Pressable>} />{focusPack ? <Pressable onPress={() => openPack(focusPack, true)} accessibilityRole="button" style={({ pressed }) => [styles.reviewButton, { borderColor: PACK_ACCENTS[focusPack.id] ?? colors.primary }, pressed && styles.pressed]}><Text style={[styles.reviewButtonText, { color: PACK_ACCENTS[focusPack.id] ?? colors.primary }]}>Open saved review for focus subject</Text><Text style={[styles.actionChevron, { color: PACK_ACCENTS[focusPack.id] ?? colors.primary }]}>›</Text></Pressable> : null}</View><Text style={[styles.subjectHeading, { color: colors.muted }]}>CORE SUBJECTS · {ready ? "TAP A SUBJECT TO START OR ADD IT OFFLINE" : "CHECKING DEVICE"}</Text></View>}
        renderItem={({ item }) => { const snapshot = courseRoundSnapshot(item.id, roundState); const installed = isPackInstalled(item, installs); const accent = PACK_ACCENTS[item.id] ?? colors.primary; return <Pressable onPress={() => openPack(item)} accessibilityRole="button" accessibilityLabel={`Open ${item.title}`} style={({ pressed }) => [styles.subjectCard, { borderColor: accent, backgroundColor: colors.surface }, pressed && styles.pressed]}><View style={[styles.subjectDot, { backgroundColor: accent }]} /><Text numberOfLines={2} style={[styles.subjectTitle, { color: colors.foreground }]}>{item.title.replace("High School ", "")}</Text><Text style={[styles.subjectState, { color: installed ? colors.success : colors.muted }]}>{installed ? "READY OFFLINE" : "ADD TO DEVICE"}</Text><Text numberOfLines={2} style={[styles.subjectCopy, { color: colors.muted }]}>{snapshot.completed}/{item.courses.length} reviewed · {snapshot.saved} saved</Text><Text style={[styles.subjectAction, { color: accent }]}>{installed ? "Start round ›" : "Open pack ›"}</Text></Pressable>; }}
        ListFooterComponent={<View style={[styles.boundary, { borderColor: colors.border, backgroundColor: colors.surface }]}><Text style={[styles.boundaryTitle, { color: colors.foreground }]}>Designed for your learning journey</Text><Text style={[styles.boundaryText, { color: colors.muted }]}>Rounds provides original reviewed starter learning and private progress. It does not replace school teaching, issue grades, predict examinations, or represent official NCDC or UNEB coverage.</Text></View>}
        ListEmptyComponent={<View style={styles.loading}><ActivityIndicator color={colors.primary} /></View>}
      />
    </ScreenContainer>
  );
}

const styles = StyleSheet.create({
  content: { paddingTop: 16, paddingBottom: 34, gap: 10 }, header: { gap: 12, marginBottom: 2 }, eyebrow: { fontSize: 10, letterSpacing: 1.6, fontWeight: "900" }, title: { fontFamily: "Georgia", fontSize: 30, lineHeight: 37, fontWeight: "700" }, sub: { fontSize: 13, lineHeight: 19 }, levelSheet: { borderWidth: 1, borderRadius: 21, padding: 15, gap: 6 }, levelLabel: { fontSize: 9, letterSpacing: 1.2, fontWeight: "900" }, levelTitle: { fontSize: 19, fontWeight: "900" }, levelNote: { fontSize: 11, lineHeight: 16 }, levelList: { gap: 7, paddingTop: 6 }, levelChip: { width: 92, minHeight: 59, borderWidth: 1, borderRadius: 14, paddingHorizontal: 10, justifyContent: "center", gap: 3 }, levelChipTitle: { fontSize: 12, fontWeight: "900" }, levelChipBand: { fontSize: 8, lineHeight: 11, fontWeight: "800" }, progressStrip: { borderWidth: 1, borderRadius: 18, padding: 13, flexDirection: "row", justifyContent: "space-between" }, progressNumber: { fontFamily: "Georgia", fontSize: 22, fontWeight: "700" }, progressLabel: { fontSize: 8, letterSpacing: 0.8, fontWeight: "900", marginTop: 2 }, actionRow: { flexDirection: "row", gap: 9 }, libraryButton: { flex: 1.45, minHeight: 47, borderWidth: 1, borderRadius: 15, paddingHorizontal: 13, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, libraryButtonText: { fontSize: 12, fontWeight: "900" }, profileButton: { flex: 1, minHeight: 47, borderWidth: 1, borderRadius: 15, paddingHorizontal: 12, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, profileButtonText: { fontSize: 12, fontWeight: "900" }, actionChevron: { fontSize: 22, fontWeight: "300" }, revisionSheet: { borderWidth: 1, borderRadius: 20, padding: 14, gap: 9 }, revisionTop: { flexDirection: "row", justifyContent: "space-between", gap: 10, alignItems: "flex-start" }, revisionLabel: { fontSize: 9, letterSpacing: 1.1, fontWeight: "900" }, revisionTitle: { fontSize: 15, lineHeight: 20, fontWeight: "900", marginTop: 2, maxWidth: 170 }, targetButton: { borderWidth: 1, borderRadius: 12, paddingHorizontal: 9, paddingVertical: 8 }, targetButtonText: { fontSize: 10, fontWeight: "900" }, revisionList: { gap: 7 }, revisionChip: { borderWidth: 1, borderRadius: 12, paddingHorizontal: 10, paddingVertical: 8 }, revisionChipText: { fontSize: 10, fontWeight: "900" }, reviewButton: { minHeight: 42, borderWidth: 1, borderRadius: 13, paddingHorizontal: 12, flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, reviewButtonText: { fontSize: 12, fontWeight: "900" }, subjectHeading: { fontSize: 10, letterSpacing: 1.25, fontWeight: "900", marginTop: 2 }, columns: { gap: 10 }, subjectCard: { flex: 1, minHeight: 164, borderWidth: 1, borderRadius: 19, padding: 13, gap: 5 }, subjectDot: { width: 26, height: 6, borderRadius: 99 }, subjectTitle: { fontSize: 15, lineHeight: 19, fontWeight: "900", marginTop: 1 }, subjectState: { fontSize: 8, letterSpacing: 0.8, fontWeight: "900" }, subjectCopy: { flex: 1, fontSize: 11, lineHeight: 15 }, subjectAction: { fontSize: 11, fontWeight: "900" }, boundary: { borderWidth: 1, borderRadius: 18, padding: 15, gap: 5, marginTop: 2 }, boundaryTitle: { fontSize: 14, fontWeight: "900" }, boundaryText: { fontSize: 12, lineHeight: 18 }, loading: { paddingVertical: 34, alignItems: "center" }, pressed: { opacity: 0.78, transform: [{ scale: 0.98 }] },
});
